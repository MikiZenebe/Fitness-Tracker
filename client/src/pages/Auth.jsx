import { AuthContainer, AuthLeft, AuthRight } from "../styles/styles";

export default function Auth() {
  return (
    <AuthContainer>
      <AuthLeft>Left</AuthLeft>
      <AuthRight>Right</AuthRight>
    </AuthContainer>
  );
}
